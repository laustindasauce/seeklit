// import { createCookie } from "@remix-run/node"; // or cloudflare/deno

// export const absToken = createCookie("abs-token", {
//   maxAge: 60 * 60 * 24 * 7, // 7 days
//   httpOnly: true, // Makes the cookie accessible only through HTTP, not JavaScript
//   secure: process.env.NODE_ENV === "production", // Only send cookies over HTTPS in production
//   sameSite: "lax", // Controls the sending of the cookie with cross-site requests
//   path: "/", // Cookie will be available across your entire site
// });
