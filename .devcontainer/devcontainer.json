{
  "name": "Seeklit",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspaces/seeklit",

  "forwardPorts": [80, 3000, 8416],

  "postCreateCommand": "cd client && pnpm install && echo 'VITE_API_URL=\"http://localhost:8416\"' > .env && echo 'SEEKLIT_ABS_URL=\"\"' >> .env && echo 'VITE_ABS_URL=\"\"' >> .env && echo 'VITE_ADMIN_EMAIL=\"dev@gmail.com\"' >> .env && cd ../server && GOPROXY=direct go mod tidy && cp -n default.conf dev.conf && echo 'SEEKLIT_CONF_FILE=\"dev.conf\"' > .env && echo 'SEEKLIT_VERSION=\"develop\"' >> .env",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.go",
        "esbenp.prettier-vscode",
        "streetsidesoftware.code-spell-checker",
        "cardinal90.multi-cursor-case-preserve",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },

  "portsAttributes": {
    "80": {
      "label": "Nginx (Combined App)",
      "onAutoForward": "notify"
    },
    "3000": {
      "label": "Client Dev Server",
      "onAutoForward": "notify"
    },
    "8416": {
      "label": "Go API Server",
      "onAutoForward": "notify"
    }
  },

  "remoteUser": "vscode"
}
